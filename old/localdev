#brew install npm
#npm install -g @devcontainers/cli

docker: use new virtualization framework
        enable virtiofs
        add --platform amd/linux to bin/dev
        run bin/dev
        -- mounts /workspaces/gemini

mkdir cpp_home_dev ?
mkdir .config/raptor ?

cd git/gemini/cpp
#devcontainer up --workspace-folder .
