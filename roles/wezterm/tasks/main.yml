- name: Configure wezterm A
  ansible.builtin.debug:
    msg: Debug for configure wezterm

- name: Configure wezterm
  ansible.builtin.copy:
    src: "wezterm.lua"
    dest: "{{ wezterm_config_home }}/wezterm.lua"
    backup: true
